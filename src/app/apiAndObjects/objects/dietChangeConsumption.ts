import { BaseObject } from '../_lib_code/objects/baseObject';

export class DietChangeConsumption extends BaseObject {
  public static readonly KEYS = {};

  protected constructor(sourceObject?: Record<string, unknown>) {
    super(sourceObject);
  }
}
